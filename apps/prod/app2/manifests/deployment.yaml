apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: app2-prod
spec:
  selector:
    matchLabels:
      app: app2
  replicas: 4
  template:
    metadata:
      labels:
        app: app2
    spec:
      containers:
      - name: app2
        image: nanajanashia/argocd-app:1.2
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        ports:
        - containerPort: 8080
